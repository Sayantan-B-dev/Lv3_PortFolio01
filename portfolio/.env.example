# Public + server config (never real secrets here).

# --- Existing (already in .env.local) ---
# MONGODB_URI=
# MONGODB_DB_NAME=
# MONGODB_PORTFOLIO_COLLECTION=blog_posts
# ADMIN_USERNAME=
# ADMIN_PASSWORD=
# CLOUDINARY_CLOUD_NAME=
# CLOUDINARY_KEY=
# CLOUDINARY_SECRET=

# --- Required new: long random string, 32+ chars ---
# Generate with: node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
# JWT_SECRET=
